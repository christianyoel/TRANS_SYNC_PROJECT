// Valores de PRODUCCIÓN.
// Durante `ng serve` este archivo es reemplazado por environment.development.ts
// gracias a la configuración fileReplacements en angular.json.
export const environment = {
  production: true,
  apiUrl: 'https://api.trans-sync.com', // URL del gateway en producción
};
