// Valores de PRODUCCIÓN (referenciado explícitamente por angular.json).
// Este archivo se usa cuando se ejecuta `ng build --configuration production`.
export const environment = {
  production: true,
  apiUrl: 'https://api.trans-sync.com', // URL del gateway en producción
};
